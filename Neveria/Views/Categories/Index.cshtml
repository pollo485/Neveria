@model IEnumerable<Neveria.Models.dbFreezeDream.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCategorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionCategorie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCategorie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionCategorie)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TagCategorie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TagCategorie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TagCategorie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
